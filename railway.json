{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn api_server:app --bind 0.0.0.0:$PORT --workers 2",
    "healthcheckPath": "/api/labs",
    "restartPolicyType": "ON_FAILURE"
  }
}
